[package]
name = "pbt"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
# Property-Based Testing (noprop はマクロなしの命令的なプロパティテストライブラリ)
noprop = "0.2"
# PBT 対象の HTTP/2 Sans I/O コア
shiguredo_http2 = { path = "../" }

[dev-dependencies]
