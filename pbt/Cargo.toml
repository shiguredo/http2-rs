[package]
name = "pbt"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
proptest = "1.9"

[dev-dependencies]
shiguredo_http2 = { path = "../" }
# Bytes 化された Frame / Event / Capsule の値を構築するために使用する
bytes = { version = "1.11", default-features = false }
